{
    "rules": {
        "no-unused-vars": "warn",
        "no-undef": "error",
        "no-console": "off"
    },

    "globals": {
        "globals": "readonly" // Add the necessary global variables, if any
    }
}
